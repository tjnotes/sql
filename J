receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "0.0.0.0:4317"
        tls:
          cert_file: "C:\\path\\to\\cert.pem"
          key_file: "C:\\path\\to\\key.pem"

processors:
  batch:

exporters:
  debug:
    verbosity: normal

extensions:
  pprof: {}        # Valid profiling extension
  zpages: {}       # Valid debugging zPages
  jaeger_storage: # Configures in-memory trace storage
    backends:
      in_memory:
        memory:
          max_traces: 100000
  jaeger_query:   # Enables Jaeger UI and query API
    storage:
      traces: in_memory
    base_path: /
    http:
      endpoint: "0.0.0.0:16686"

service:
  extensions: [pprof, zpages, jaeger_query]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]
