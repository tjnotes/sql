receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "0.0.0.0:4317"
        tls:
          cert_file: "C:\\path\\to\\cert.pem"
          key_file: "C:\\path\\to\\key.pem"

processors:
  batch:

exporters:
  debug:
    verbosity: normal

extensions:
  pprof: {}        # Valid in Jaeger 2.8
  zpages: {}       # Valid in Jaeger 2.8
  jaeger_storage:
    backends:
      memory_storage:
        memory:
          max_traces: 100000
  jaeger_query:
    storage:
      traces: memory_storage
    base_path: /
    http:
      endpoint: "0.0.0.0:16686"

service:
  extensions: [pprof, zpages, jaeger_query]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]
