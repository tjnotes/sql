openssl pkcs12 -export -in cert.pem -inkey key.pem -out jaeger-cert.p12 -name jaeger

# Import into Java truststore (default password is 'changeit')
keytool -import -trustcacerts -alias jaeger-collector -file cert.pem -keystore $JAVA_HOME/jre/lib/security/cacerts
