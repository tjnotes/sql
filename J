receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "0.0.0.0:4317"
        tls:
          cert_file: "C:\\path\\to\\cert.pem"
          key_file: "C:\\path\\to\\key.pem"

processors:
  batch:

exporters:
  debug:
    verbosity: normal

extensions:
  health_check: {}         # Valid extension
  pprof: {}                # Valid extension
  zpages: {}               # Valid extension
  jaeger_storage:         # Required for storage
    backends:
      in_memory:
        memory:
          max_traces: 100000
  jaeger_query:            # Required to expose UI and query API
    storage:
      traces: in_memory

service:
  extensions: [health_check, pprof, zpages, jaeger_query]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]
